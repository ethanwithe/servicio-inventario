spring:
  application:
    name: servicio-inventario
    
  datasource:
    url: jdbc:mysql://shuttle.proxy.rlwy.net:36963/railway?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: CNYmHbjbfcgyLirDYrDzCsSlYswCOOSh
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000
    
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    sql:
      init:     # ⬅ Ejecuta data.sql siempre
        data-locations: classpath:data_inventario.sql
        encoding: UTF-8     # ⬅ Evita errores de caracteres
      
server:
  port: ${PORT:8083}
  servlet:
    context-path: /

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server.railway.internal:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    hostname: servicio-inventario.railway.internal
    instance-id: ${spring.application.name}:${random.value}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
